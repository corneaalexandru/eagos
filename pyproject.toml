[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "eagos"
version = "5.1.1"
description = "Evidence-led Agent Governance and Operations System"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["eagos"]
